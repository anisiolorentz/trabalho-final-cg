Make it so it is possible to rotate the objects the user is holding using the R key in the keyboard. Currently, that key is being used to reload shaders. Reloading shaders should now be executed when pressing the L key. The user must have to hold the R key in order to the object to rotate. It should happen in a somewhat slow speed, so it's easy to adjust the rotation angle.